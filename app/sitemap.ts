import type { MetadataRoute } from "next"; import { getAllPosts } from "@/lib/posts";
export default function sitemap(): MetadataRoute.Sitemap { const base = process.env.NEXT_PUBLIC_SITE_URL ?? "http://localhost:3000"; return ["", "/posts", "/tags", "/about"].map(url => ({ url: `${base}${url}`, lastModified: new Date() })).concat(getAllPosts().map(p => ({ url: `${base}/posts/${p.slug}`, lastModified: new Date(p.date) }))); }
