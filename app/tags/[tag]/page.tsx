import { notFound } from "next/navigation"; import { getPostsByTag } from "@/lib/posts"; import PostList from "@/components/PostList";
export default async function TagPage({ params }: { params: Promise<{tag: string}> }) { const { tag: encodedTag } = await params; let tag: string; try { tag = decodeURIComponent(encodedTag); } catch { notFound(); } return <><h1>#{tag}</h1><PostList posts={getPostsByTag(tag)} /></>; }
