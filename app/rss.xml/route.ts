import { getAllPosts } from "@/lib/posts";
export function GET() { const base = process.env.NEXT_PUBLIC_SITE_URL ?? "http://localhost:3000"; const items = getAllPosts().map(p => `<item><title><![CDATA[${p.title}]]></title><link>${base}/posts/${p.slug}</link><guid>${base}/posts/${p.slug}</guid><pubDate>${new Date(p.date).toUTCString()}</pubDate><description><![CDATA[${p.description}]]></description></item>`).join(""); const xml = `<?xml version="1.0" encoding="UTF-8"?><rss version="2.0"><channel><title>${process.env.NEXT_PUBLIC_SITE_NAME ?? "My Notes"}</title><link>${base}</link><description>Personal blog</description>${items}</channel></rss>`; return new Response(xml, { headers: { "Content-Type": "application/rss+xml; charset=utf-8" } }); }
