import Link from "next/link"; import { formatPostDate, type Post } from "@/lib/posts";
export default function PostList({ posts }: { posts: Post[] }) { return <div>{posts.map(post => <article className="post-card" key={post.slug}><p className="muted">发布于 {formatPostDate(post.publishedAt)} · 更新于 {formatPostDate(post.updatedAt)} · {post.readingTime}</p><h2><Link href={`/posts/${post.slug}`}>{post.title}</Link></h2><p>{post.description}</p>{post.tags.map(t => <Link className="tag" key={t} href={`/tags/${encodeURIComponent(t)}`}>#{t}</Link>)}</article>)}</div>; }
