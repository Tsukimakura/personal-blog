import type { MetadataRoute } from "next"; import { getAllPosts } from "@/lib/posts"; import { site } from "@/lib/site";
export default function sitemap(): MetadataRoute.Sitemap { return ["", "/posts", "/archive", "/tags", "/about"].map(url => ({ url: `${site.url}${url}`, lastModified: new Date() })).concat(getAllPosts().map(p => ({ url: `${site.url}/posts/${p.slug}`, lastModified: new Date(p.updatedAt) }))); }
